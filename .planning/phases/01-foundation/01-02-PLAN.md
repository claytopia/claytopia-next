---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - app/components/Header.tsx
  - app/components/Footer.tsx
  - app/components/Container.tsx
  - app/layout.tsx
  - app/page.tsx
autonomous: true

must_haves:
  truths:
    - "Header is sticky and stays visible when scrolling"
    - "Header shows Claytopia wordmark on the left"
    - "Footer displays at the bottom of all pages"
    - "Content is centered with consistent max-width and padding"
    - "Layout is responsive: looks good on 375px mobile and 1440px desktop"
  artifacts:
    - path: "app/components/Header.tsx"
      provides: "Sticky header with logo placeholder and hamburger placeholder"
      min_lines: 15
    - path: "app/components/Footer.tsx"
      provides: "Minimal footer with contact info placeholders"
      min_lines: 15
    - path: "app/components/Container.tsx"
      provides: "Responsive container with max-width and padding"
      min_lines: 8
  key_links:
    - from: "app/layout.tsx"
      to: "app/components/Header.tsx"
      via: "import and render"
      pattern: "import.*Header"
    - from: "app/layout.tsx"
      to: "app/components/Footer.tsx"
      via: "import and render"
      pattern: "import.*Footer"
---

<objective>
Create the core layout components (Header, Footer, Container) that will wrap all pages, establishing the consistent visual structure visitors experience.

Purpose: Build the page skeleton that navigation (Plan 03) and content (Phase 2) will populate.
Output: Working Header with logo placeholder, Footer with minimal structure, Container for content centering, all integrated into layout.tsx
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@app/globals.css
@app/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Container component</name>
  <files>app/components/Container.tsx</files>
  <action>
Create the components directory and Container component:

```tsx
// app/components/Container.tsx
interface ContainerProps {
  children: React.ReactNode;
  className?: string;
}

export function Container({ children, className = '' }: ContainerProps) {
  return (
    <div className={`mx-auto w-full max-w-6xl px-4 sm:px-6 lg:px-8 ${className}`}>
      {children}
    </div>
  );
}
```

The Container:
- Centers content with mx-auto
- Sets max-width to 6xl (72rem / 1152px) - comfortable reading width
- Applies responsive horizontal padding: 16px mobile, 24px tablet, 32px desktop
- Accepts className for additional styling when needed
  </action>
  <verify>
File exists at app/components/Container.tsx with TypeScript interface and responsive padding classes
  </verify>
  <done>
Container component created with responsive padding and max-width constraints
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Header and Footer components</name>
  <files>app/components/Header.tsx, app/components/Footer.tsx</files>
  <action>
Create Header component (app/components/Header.tsx):

```tsx
// app/components/Header.tsx
import { Container } from './Container';

export function Header() {
  return (
    <header className="sticky top-0 z-40 w-full border-b border-foreground/10 bg-background/95 backdrop-blur supports-[backdrop-filter]:bg-background/80">
      <Container>
        <div className="flex h-16 items-center justify-between">
          {/* Logo/wordmark - left */}
          <a href="/" className="text-xl font-semibold tracking-tight text-foreground">
            Claytopia
          </a>

          {/* Hamburger placeholder - will be replaced by MobileMenu in Plan 03 */}
          <button
            type="button"
            className="flex h-10 w-10 items-center justify-center rounded-md text-foreground hover:bg-background-alt"
            aria-label="Menu"
          >
            {/* Simple hamburger icon placeholder */}
            <svg
              className="h-6 w-6"
              fill="none"
              viewBox="0 0 24 24"
              stroke="currentColor"
              strokeWidth={2}
            >
              <path strokeLinecap="round" strokeLinejoin="round" d="M4 6h16M4 12h16M4 18h16" />
            </svg>
          </button>
        </div>
      </Container>
    </header>
  );
}
```

Header features:
- Sticky positioning with z-index
- Subtle bottom border using foreground at 10% opacity
- Semi-transparent background with backdrop blur (glassmorphism effect)
- Logo as text link to home
- Hamburger button placeholder (Plan 03 will replace with hamburger-react)

Create Footer component (app/components/Footer.tsx):

```tsx
// app/components/Footer.tsx
import { Container } from './Container';

export function Footer() {
  return (
    <footer className="border-t border-foreground/10 bg-background-alt py-8">
      <Container>
        <div className="flex flex-col items-center gap-4 text-center sm:flex-row sm:justify-between sm:text-left">
          {/* Contact info */}
          <div className="text-sm text-foreground-muted">
            <p className="font-medium text-foreground">Claytopia</p>
            <p>Keramik-Atelier</p>
          </div>

          {/* Quick links placeholder - will be populated in Phase 2 */}
          <nav className="flex gap-4 text-sm text-foreground-muted">
            <a href="/impressum" className="hover:text-foreground">Impressum</a>
            <a href="/datenschutz" className="hover:text-foreground">Datenschutz</a>
          </nav>
        </div>

        <div className="mt-6 text-center text-xs text-foreground-muted">
          &copy; {new Date().getFullYear()} Claytopia. Alle Rechte vorbehalten.
        </div>
      </Container>
    </footer>
  );
}
```

Footer features:
- Subtle top border for separation
- Uses background-alt (sage-100) for visual distinction
- Responsive layout: stacked on mobile, row on tablet+
- Placeholder contact info and required legal links (Impressum, Datenschutz)
- Dynamic copyright year
- German text as specified
  </action>
  <verify>
Both files exist:
- app/components/Header.tsx (sticky, logo, hamburger placeholder)
- app/components/Footer.tsx (contact placeholder, legal links)
No TypeScript errors when running `npm run dev`
  </verify>
  <done>
Header component with sticky positioning, logo, and hamburger placeholder created. Footer component with contact area and legal links created.
  </done>
</task>

<task type="auto">
  <name>Task 3: Integrate layout components and update page</name>
  <files>app/layout.tsx, app/page.tsx</files>
  <action>
Update layout.tsx to include Header and Footer:

1. Import Header and Footer:
   ```tsx
   import { Header } from './components/Header';
   import { Footer } from './components/Footer';
   ```

2. Wrap children in a flex layout structure:
   ```tsx
   <body className="...existing classes...">
     <div className="flex min-h-screen flex-col">
       <Header />
       <main className="flex-1">
         {children}
       </main>
       <Footer />
     </div>
   </body>
   ```

This structure:
- Uses flex-col to stack Header, main, Footer
- min-h-screen ensures full viewport height
- flex-1 on main pushes Footer to bottom when content is short

Update page.tsx to use Container and test layout:

```tsx
import { Container } from './components/Container';

export default function Home() {
  return (
    <Container className="py-12">
      <h1 className="text-3xl font-semibold mb-4">Willkommen bei Claytopia</h1>
      <p className="text-foreground-muted mb-8">
        Dein Keramik-Atelier für kreative Töpfer-Erlebnisse.
      </p>

      {/* Test scroll behavior - enough content to enable scrolling */}
      <div className="space-y-8">
        {[1, 2, 3, 4, 5].map((i) => (
          <div key={i} className="p-6 bg-background-alt rounded-lg">
            <h2 className="text-xl font-medium mb-2">Abschnitt {i}</h2>
            <p className="text-foreground-muted">
              Dieser Platzhalter-Inhalt testet das Scroll-Verhalten des sticky Headers.
              Bei ausreichend Inhalt sollte der Header beim Scrollen sichtbar bleiben.
            </p>
          </div>
        ))}
      </div>
    </Container>
  );
}
```

This test page:
- Uses Container for proper content centering
- Has German placeholder text
- Creates enough content to test sticky header scrolling
- Tests background-alt color for content cards
  </action>
  <verify>
Run `npm run dev` and verify at http://localhost:3000:
1. Header appears at top with "Claytopia" logo and hamburger icon
2. Header has subtle glassmorphism effect (semi-transparent with blur)
3. Scroll down - header stays sticky at top
4. Footer appears at bottom with contact placeholder and legal links
5. If content is short, Footer is still at bottom (not floating mid-page)
6. Content is centered with padding on both mobile (375px) and desktop (1440px) viewports
7. Responsive: check at 375px width - layout doesn't break
  </verify>
  <done>
Layout.tsx integrates Header and Footer with flex structure. Page.tsx uses Container and displays German placeholder content. Sticky header and footer-at-bottom work correctly.
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `npm run dev` starts without errors
2. Visit http://localhost:3000:
   - Header visible at top with "Claytopia" wordmark and hamburger icon
   - Scrolling keeps header sticky
   - Content is centered with proper padding
   - Footer at bottom with legal links
3. Test responsive behavior:
   - At 375px: content has 16px padding, footer stacks vertically
   - At 1440px: content has max-width ~1152px centered, footer is horizontal
4. All components use design tokens from Plan 01 (bg-background, text-foreground, etc.)
</verification>

<success_criteria>
- Header is sticky with glassmorphism effect
- Header shows "Claytopia" on left, hamburger icon on right
- Footer has contact placeholder and Impressum/Datenschutz links
- Footer uses background-alt color (sage-100)
- Container centers content with responsive padding
- Flex layout ensures footer stays at bottom
- All text is in German
- Layout works on 375px mobile and 1440px desktop viewports
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
